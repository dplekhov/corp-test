{% extends 'base.html.twig' %}

{% block body %}
    <h1>Value</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ value.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ value.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('value_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('value_edit', { 'id': value.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
